1. Prompt user for height, h
2. If height is less than one or greater than zero, then go back to step 1
3. Iterate from 1 to h
    1. On iteration i, print hashes in such a way that the row number is never less than the column number(print i hashes)
    2. Go to next line